# CHANGELOG

## 0.1.1 - 2026-08-23

### 新增功能

- 本地开发时，官方 OpenAI 地址自动走 Vite 代理，减少浏览器跨域失败。
- 设置页增加“测试连接”，并说明官方接口不宜在打包后的网页里直连。
- 导入备份前会确认将替换的记录数和错题数；写入失败会尽量回滚。
- 离开拍照页或刷新时，未保存的识别结果会提示确认。
- 识别中可以取消；超时改为 60 秒。
- 学习记录可删除；正确率只统计已判定题目，并单独显示待确认数量。
- 错题本按全部知识点筛选；删除错题后，对应学习记录不再显示“已入错题本”。
- 设置页可测试连接，并显示本地已用空间。
- 上线到 Vercel 后，官方 OpenAI 走同域转发，手机在外网也可识别。

### 修复问题

- 跨域或请求根本没发出去时，不再误报成普通网络错误。
- 学习记录不再保存原图；打开记录时会清掉旧数据里残留的原图。
- 保存错题改为先写入错题本，避免配额失败留下半成品。
- iPhone HEIC 无法读取时给出明确提示。
- 上传失败改为错误提示，而不是普通通知。
- 导入缺少字段时补默认值，避免错题详情页崩溃。

### 已知问题

- 错题原图仍保存在各设备自己的浏览器里，换手机不会自动同步，空间不够时请先导出再删旧题。

## 0.1.0 - 2026-08-20

### 新增功能

- 提供适合家长陪同使用的首页，入口为拍照/上传作业、错题本、学习记录。
- 设置页支持填写 API Key、模型名称和接口地址；API Key 只保存在浏览器本地。
- 支持 jpg/png 上传、手机拍照、图片预览、自动压缩和重新上传确认。
- 调用多模态 AI 识别数学题，返回结构化结果，并允许家长修改题目、学生答案、参考答案、知识点和对错判断。
- 讲解支持引导模式和答案模式。
- 辅助批改支持正确、错误、部分正确、无法判断、需家长确认；错误题可标记错因。
- 内置人教版四年级上册常见知识点清单。
- 错题本支持保存、筛选、搜索、详情和复习状态修改。
- 学习记录统计总题数、正确数、错误数、正确率、近 7 天数量和知识点错题数。
- 支持学习记录和错题本的 JSON 导出/导入。
- 页面提示：作业图片将发送给 AI 服务；清除浏览器缓存可能导致数据丢失。

### 修复问题

- 无。本版本为初始版本。

### 已知问题

- 识别效果依赖用户自己的多模态模型和图片清晰度。
- 第一期不会自动判断照片是否模糊、过暗或倾斜。
- 数据只存在当前浏览器，换设备不会自动同步。
- 不会完整覆盖四年级上册所有教材原题。
- 大图片压缩后可能损失细节，需要重新拍摄。
